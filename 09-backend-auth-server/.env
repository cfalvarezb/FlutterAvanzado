# Environment Config

# store your secrets and config variables in here
# only invited collaborators will be able to see your .env values
# Note: comments will be visible to remixes so dont put secrets in comments!

# reference these in your code with process.env.SECRET

# Found in the top-right corner of https://developer.apple.com/account/resources/certificates/list
# under your name
TEAM_ID=YOUR_TEAM

# The service's identifier
# Go to https://developer.apple.com/account/resources/identifiers/list/serviceId, click in the service
# and copy the "Identifier" value
SERVICE_ID=com.example.service

# The Bundle ID of your app
BUNDLE_ID=com.example.service

# The ID of your app
# Go to https://developer.apple.com/account/resources/authkeys/list and select the
# key you created for sign in with apple
KEY_ID=6W9N8GD6BU

# The contents of service key downloaded during the service creation
# Replace all line-breaks with '|'
KEY_CONTENTS=

# Your package indentifier from the Google Play Store
# e.g com.example.app
# For the example app provided with the plugin, use 'com.aboutyou.dart_package.sing_in_apple_examp'
ANDROID_PACKAGE_IDENTIFIER=com.example.service

# note: .env is a shell file so there can´t be spaces around